# ReelVault
ReelVault is a React web app that lets users upload their Instagram export ZIP and browse saved reels collection-wise in a clean, responsive interface. The app parses Instagram JSON data locally in the browser, supports collection filtering and caption search, and provides quick access to saved posts without storing any user data on a server.
